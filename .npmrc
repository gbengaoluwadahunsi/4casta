# Use hoisted node_modules so standalone Node scripts (e.g. scripts/clear-forecasts-and-budget.mjs)
# can resolve @supabase/supabase-js when run with `node scripts/clear-forecasts-and-budget.mjs`.
# With the default pnpm layout, ESM resolution from scripts in project root fails on Windows.
node-linker=hoisted
